let sentence = "This is demo texts for using string methods";
let index = 5;

// charAt() for return character specified index(position) in a string

console.log(`The charector at index ${index} is ${sentence.charAt(index)}`);

